[package]
name = "downfall"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.17.3", features = ["experimental_bevy_ui_widgets", "dynamic_linking"] }
